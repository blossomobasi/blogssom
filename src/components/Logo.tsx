import { Link } from "react-router-dom";

const Logo = () => {
    return (
        <Link to="/" className="xl font-semibold">
            <span>✈</span>Blogssom <span>🧳</span>
        </Link>
    );
};

export default Logo;
